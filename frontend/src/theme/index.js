/* ============================================================
   EDUMIND AI — THEME BARREL EXPORT
   ============================================================ */

export { colors, getScoreColor, getGradeLabel, getLeadColor } from './colors'
export { typography } from './typography'

export const theme = {
  borderRadius: '0px',        // Brutalist — no radius
  borderWidth: '3px',
  transition: '0.15s ease',
  transitionSlow: '0.3s ease',
  headerHeight: '52px',
  sidebarWidth: '320px',
  navWidth: '240px',
}

export default theme
